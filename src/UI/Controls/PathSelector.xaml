<UserControl x:Class="SekaiLayer.UI.Controls.PathSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <TextBox Grid.Column="0" x:Name="FilePathDisplay" IsReadOnly="True"
                 HorizontalAlignment="Stretch" Background="LightGray" BorderBrush="Black"
                 ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        <Button Grid.Column="1" HorizontalAlignment="Stretch" Margin="5 0 0 0"
                Click="SelectPathOnClick" Content="Select" />
    </Grid>
</UserControl>
