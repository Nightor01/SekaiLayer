<Window x:Class="SekaiLayer.UI.Windows.SelectGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Group Selection Dialog" Height="100" Width="250"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Icon="{StaticResource AppIcoIcon}">
    <StackPanel>
        <Label Content="Select a group:" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="60" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" ItemsSource="{Binding Path=GroupNames}" Margin="5"
                      AllowDrop="False" x:Name="ComboBox" />
            <Button Grid.Column="1" Margin="0 5 5 5" Content="Ok" Click="Ok_OnClick" />
        </Grid>
    </StackPanel>
</Window>
