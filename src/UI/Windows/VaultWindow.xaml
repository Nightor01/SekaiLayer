<Window x:Class="SekaiLayer.UI.Windows.VaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:controls="clr-namespace:SekaiLayer.UI.Controls"
        mc:Ignorable="d"
        Icon="{StaticResource AppIcoIcon}"
        Title="{StaticResource AppTitle}" Height="450" Width="800"
        MinWidth="400" MinHeight="400" Loaded="VaultWindow_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" MinWidth="100" MaxWidth="600">
                <ColumnDefinition.Style>
                    <Style TargetType="ColumnDefinition">
                        <Style.Triggers>
                            <DataTrigger
                                Binding="{Binding IsLoaded, RelativeSource={RelativeSource AncestorType=Grid}}"
                                Value="True">
                                <Setter Property="Width" Value="*" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ColumnDefinition.Style>
            </ColumnDefinition>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid Grid.Row="0" Column="0" ColumnSpan="3" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="210" />
                <ColumnDefinition Width="2" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <GroupBox Grid.Column="0" Header="Resources">
                <StackPanel Orientation="Horizontal">
                    <Button Width="50" Height="50" ToolTip="Import a resource" Click="AddResourceOnClick">
                        <Image Source="{StaticResource DiAddIcon}" Margin="5" />
                    </Button>
                    <StackPanel Orientation="Vertical" Margin="5 3.5 5 3.5">
                        <Button Content="Add Existing" Height="22.5" Margin="0 0 0 5"
                                Click="AddExisting_OnClick"
                                ToolTip="Adds a resource imported manually into Sekai-Layer" />
                        <Button Content="Add Default" Height="22.5" Margin="0"
                                Click="AddDefault_OnClick" ToolTip="Adds an asset group from app defaults" />
                    </StackPanel>
                </StackPanel>
            </GroupBox>
            
            <Border Grid.Column="1" Background="LightSlateGray" />
        </Grid>
        
        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                      Background="Black" Height="2" VerticalAlignment="Center"
                      HorizontalAlignment="Stretch" Margin="0"/>
        
        <TreeView Grid.Row="2" Grid.Column="0" x:Name="TreeView" 
                  Margin="1" BorderThickness="0" />
        
        <GridSplitter Grid.Row="2" Grid.Column="1"
                      VerticalAlignment="Stretch" 
                      HorizontalAlignment="Center">
            <GridSplitter.Style>
                <Style TargetType="GridSplitter">
                    <Setter Property="Width" Value="2"/>
                    <Setter Property="Background" Value="Black"/>
                    <Setter Property="Margin" Value="6 0 0 0" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width"
                                                         To="8" Duration="0:0:0.15" BeginTime="0:0:0.01"
                                                         FillBehavior="HoldEnd" />
                                        <ColorAnimation Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                                        To="DarkSlateBlue" Duration="0:0:0.15"
                                                        BeginTime="0:0:0.01"/>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                            To="0" Duration="0:0:0.15" BeginTime="0:0:0.01" 
                                                            FillBehavior="HoldEnd" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width"
                                                         To="2" Duration="0:0:0.15"
                                                         FillBehavior="HoldEnd"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                                        To="Black" Duration="0:0:0.15"/>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                            To="6 0 0 0" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </GridSplitter.Style>
        </GridSplitter>
        
        <TabControl Grid.Row="2" Grid.Column="2" 
                    Margin="0" BorderThickness="0 1 0 0" 
                    x:Name="TabControl">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal"
                                d:DataContext="{d:DesignInstance Type=controls:CustomTabItem}">
                        <TextBlock Text="{Binding Header}" Margin="0 0 10 0" />
                        <Image Source="{StaticResource DiRemove}" Width="20" Height="20" MouseUp="TabItemRemove" />
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentControl d:DataContext="{d:DesignInstance Type=controls:CustomTabItem}"
                                    Content="{Binding Content}" />
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </Grid>
</Window>
