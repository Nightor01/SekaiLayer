<Window x:Class="SekaiLayer.UI.Windows.VaultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Icon="{StaticResource AppIcoIcon}"
        Title="{StaticResource AppTitle}" Height="450" Width="800"
        MinWidth="400" MinHeight="400" Loaded="VaultWindow_OnLoaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" MinWidth="100" MaxWidth="600">
                <ColumnDefinition.Style>
                    <Style TargetType="ColumnDefinition">
                        <Style.Triggers>
                            <DataTrigger
                                Binding="{Binding IsLoaded, RelativeSource={RelativeSource AncestorType=Grid}}"
                                Value="True">
                                <Setter Property="Width" Value="*" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ColumnDefinition.Style>
            </ColumnDefinition>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                      Background="Black" Height="2" VerticalAlignment="Center"
                      HorizontalAlignment="Stretch"/>
        
        <TreeView Grid.Row="2" Grid.Column="0" x:Name="TreeView" 
                  Margin="1" BorderThickness="0" />
        
        <GridSplitter Grid.Row="2" Grid.Column="1"
                      VerticalAlignment="Stretch" 
                      HorizontalAlignment="Center">
            <GridSplitter.Style>
                <Style TargetType="GridSplitter">
                    <Setter Property="Width" Value="2"/>
                    <Setter Property="Background" Value="Black"/>
                    <Setter Property="Margin" Value="8 0 2 0" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width"
                                                         To="8" Duration="0:0:0.15" BeginTime="0:0:0.01"
                                                         FillBehavior="HoldEnd" />
                                        <ColorAnimation Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                                        To="DarkSlateBlue" Duration="0:0:0.15"
                                                        BeginTime="0:0:0.01"/>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                        To="2 0 2 0" Duration="0:0:0.15" BeginTime="0:0:0.01" 
                                                        FillBehavior="HoldEnd" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Width"
                                                         To="2" Duration="0:0:0.15"
                                                         FillBehavior="HoldEnd"/>
                                        <ColorAnimation Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                                                        To="Black" Duration="0:0:0.15"/>
                                        <ThicknessAnimation Storyboard.TargetProperty="Margin"
                                                        To="8 0 2 0" Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </GridSplitter.Style>
        </GridSplitter>
    </Grid>
</Window>
